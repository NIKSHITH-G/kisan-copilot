# Copy to .env and fill in real values. .env is git-ignored — NEVER commit it.

# --- Snowflake ---
# Preferred: reuse the OAuth connection already configured in ~/.snowflake/connections.toml
SNOWFLAKE_CONNECTION_NAME=HE20264
# Fallback (only used if SNOWFLAKE_CONNECTION_NAME is unset): password auth
SF_ACCOUNT=bm13081.ap-southeast-2
SF_USER=NIKKY001
SF_PASSWORD=

# --- data.gov.in (Agmarknet mandi prices) ---
# Regenerate the previously leaked key at https://data.gov.in before pasting.
DATA_GOV_KEY=

# --- Sarvam AI (voice, Phase 4) ---
SARVAM_KEY=

# --- Twilio (WhatsApp/SMS action, Phase 5) ---
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_WHATSAPP_FROM=
